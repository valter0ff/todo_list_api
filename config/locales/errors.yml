en:
  errors:
    unprocessable: Unprocessable request
    rules:
      user:
        rules:
          username:
            filled?: The field is required.
            min_size?: Username is too short. Minimum 3 characters.
            max_size?: Username is too long. Maximum 50 characters.
            unique_username?: This login is already registered. Please, log in.
          password:
            filled?: The field is required.
            size?: Password length must be 8 characters
            format?: Password does not meet minimal requirements. The length should be 8 characters, alphanumeric.
          password_confirmation:
            filled?: The field is required.
            eql?: Password and Confirm password fields does not match.
      session:
        unauthorized: Incorrect login or(and) password
      project:
        rules:
          title:
            filled?: The field is required.
            unique_title?: The project with such name does already exist.
      task:
        rules:
          name:
            filled?: The field is required.
          status:
            is_done?: The task is done. You can`t edit completed task.
          deadline:
            gteq?: Deadline cann`t be in past
            filled?: The field is required.
      comment:
        rules:
          image:
            valid_image?: File format should be `jpeg` or `png` and size should be less than 10 Mb
